include sensor
include led

mixin insertCard(card)
    - let conf = card.cardData;
    if card.type === 'sensor'
        +sensorcard(conf.title, conf.fields, conf.chart, conf.fab)
    else if card.type === 'led'
        +ledcard
